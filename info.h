/*#ifndef INFO_H
#define INFO_H

#include<iostream>
#include<string>

using namespace std;

class Info{
    //private:
    int numero;
    string nome;
    float media;

    public:
    Info(int = 0, float = 0, string = 0);     //default constructor
    bool insert_info(Info *info = NULL);
    ~Info() {};       //destructor
};

#endif // INFO_H*/
